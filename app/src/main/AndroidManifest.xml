<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.learnandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_dodo_shadow"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_dodo_shadow_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".ReferencesActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".LearnActivity" />
        <activity android:name=".HuntResultsActivity" />
        <activity android:name=".HuntInputActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".Edit2Activity" />
        <activity android:name=".Edit1Activity" />
        <activity android:name=".DeleteActivity" />
        <activity android:name=".BaseActivity" />
        <activity android:name=".AddActivity" />
        <activity android:name=".DodoListActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>